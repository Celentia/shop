import { TextEmphasisDirective } from './text-emphasis.directive';

describe('TextEmphasisDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEmphasisDirective();
    expect(directive).toBeTruthy();
  });
});
